services:
  - type: web
    name: cafeteria-guarincito
    runtime: node
    region: oregon
    plan: free
    buildCommand: npm install && NODE_OPTIONS=--max-old-space-size=512 ng build --configuration production
    startCommand: node dist/app/server/server.mjs
    envVars:
      - key: NODE_ENV
        value: production
